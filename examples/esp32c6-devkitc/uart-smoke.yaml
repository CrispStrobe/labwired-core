# LabWired - ESP32-C6-DevKitC-1 UART smoke test
schema_version: "1.0"
inputs:
  # Built by: cargo build -p firmware-esp32c6-demo --release --target riscv32imc-unknown-none-elf
  # Survival fixture (same bytes): tests/fixtures/esp32c6-demo.elf
  firmware: "../../target/riscv32imc-unknown-none-elf/release/firmware-esp32c6-demo"
  system: "./system.yaml"
limits:
  max_steps: 200000
assertions:
  - uart_contains: "OK"
  - expected_stop_reason: max_steps
